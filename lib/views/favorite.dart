import 'package:flutter/material.dart';

class Favortie extends StatelessWidget {
  const Favortie({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('favorite');
  }
}